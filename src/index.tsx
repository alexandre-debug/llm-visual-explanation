import React from 'react'
import ReactDOM from 'react-dom/client'
import LLMVisualExplanation from '../llm-visual-explanation'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LLMVisualExplanation />
  </React.StrictMode>,
)